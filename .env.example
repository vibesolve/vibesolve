# Copy this file to .env.local and fill in your key(s).
# .env.local is gitignored and must never be committed.

# Required for the default provider (openai):
OPENAI_API_KEY=

# Required only when running with --provider claude:
ANTHROPIC_API_KEY=
