# Copy this file to .env.local and fill in your key(s).
# .env.local is gitignored and must never be committed.

# Required for the default any-llm provider (openai):
OPENAI_API_KEY=

# Required only when running with --provider claude (any-llm anthropic provider):
ANTHROPIC_API_KEY=
